% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize_pr_activity_by_user.R
\name{summarize_pr_activity_by_user}
\alias{summarize_pr_activity_by_user}
\title{Summarize PR activity by user}
\usage{
summarize_pr_activity_by_user(
  repos,
  token = NULL,
  days = 365,
  use_cache = TRUE,
  cache_dir = NULL
)
}
\arguments{
\item{repos}{Character vector of repositories in the form \code{owner/repo}.}

\item{token}{Optional GitHub personal access token. Defaults to the token
discovered automatically by the gh package when \code{NULL}.}

\item{days}{Number of days to look back from today (inclusive).}

\item{use_cache}{Logical indicating whether to use local disk cache for
pull request review payloads. Defaults to \code{TRUE}.}

\item{cache_dir}{Optional cache directory path. When \code{NULL}, a platform-
appropriate default cache location is used.}
}
\value{
A data frame with columns \code{user}, \code{prs_opened}, \code{prs_reviewed}, and
\code{total_activity}.
}
\description{
\code{summarize_pr_activity_by_user()} retrieves pull requests and reviews for a
set of repositories and summarizes activity counts by GitHub user.
}
\examples{
\dontrun{
summarize_pr_activity_by_user(c("tidyverse/ggplot2"), days = 365)
}
}
